# DDR Report Generator — Python Dependencies
# Install with: pip install -r requirements.txt

pymupdf>=1.23.0        # PDF parsing and image extraction (import as fitz)
Pillow>=10.0.0         # Image processing and format conversion
python-docx>=1.1.0     # DOCX report generation
